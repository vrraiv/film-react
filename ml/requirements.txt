# No third-party dependencies are required for the baseline prototype.
# The trainer uses a small standard-library logistic regression implementation
# so it can run on newer Python versions before scikit-learn wheels are ready.
